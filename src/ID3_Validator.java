
public class ID3_Validator {

public ID3_Validator() {

}

/*public static void check_D_Pings(String[][] _curSessionPingArr) {
	// need to look at trans pings - but that's not one of the name/value pairs - left off here
	for(int i = 0; i < _curSessionPingArr.length; i++){
		if(getValByName(_curSessionPingArr[i], "trans") != null){
			bldv = ConfigChecker.getValFromUrl(urlStr, "bldv"); 
		}
		if(getValByName(_curSessionPingArr[i], "trans") != null){
			bldv = ConfigChecker.getValFromUrl(urlStr, "bldv"); 
		}

	}
}
private static String getValByName(String[] _pingArr, String _nameToGet){
	Integer i;
	String valPair[]; 
	for(i = 0; i < _pingArr.length; i++){
		valPair = _pingArr[i].split("=");
		//System.out.println(valPair[0] + ": " + valPair[1]);
		if(valPair[0].equals(_nameToGet)){
			return(valPair[1]);
		}
	}
	return(null);
}*/


}
